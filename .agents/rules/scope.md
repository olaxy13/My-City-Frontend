# Workspace Boundary Rule

- Never modify code outside the active project folder the user is currently working in.
- Do not make cross-repository or backend edits without explicit request and consent from the user.
